/**
 * Configuration Template for AKM-POS
 * 
 * SECURITY INSTRUCTIONS:
 * 1. Copy this file to 'config.js'
 * 2. Add 'config.js' to .gitignore
 * 3. Set your actual API key in config.js
 * 4. Never commit config.js to version control
 * 
 * In production, use environment variables instead.
 */

window.AKM_CONFIG = {
  // Replace with your actual proxy API key
  // This should match the AKM_PROXY_KEY in your server environment
  PROXY_KEY: 'your-secure-api-key-here',
  
  // Optional: Override API base URL for different environments
  // API_BASE_URL: 'http://localhost:3000', // Development
  // API_BASE_URL: 'https://your-production-api.com', // Production
};
